import 'package:flutter/material.dart';

class WidgetLeassonCard extends StatelessWidget {
  const WidgetLeassonCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}